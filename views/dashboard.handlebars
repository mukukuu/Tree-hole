    <div class="mb-5 pt-3">
        <a href="/dashboard/create/"><button id="create-post" class="button is-pulled-right">create post</button></a>
        
        <h2 class="title pb-4">Dashboard</h2><hr>
        <h2>Your Posts</h2>
        {{#if posts.length}}
           {{#each posts as |post|}}
        {{> post-info post}} 
    </div>

     

         <a href="/dashboard/edit/{{post.id}}"><button class="button mb-5 ml-5">Edit post</button></a>
        </div>
        {{/each}}

{{else}}
<h6 class="pb-4 has-text-centered is-italic">you do not currently have any posts</h6>

{{/if}}